let canceled=false;
let familyWorker=null;

const VIS_DB='mochimono-visual-similarity';
const VIS_VERSION=1;
const VIS_STORE='fingerprints';
const HASH_RE=/^[a-f0-9]{64}$/;
const AI_COLOR_VERSION='ai-sort-color-v1';
const FAMILY_WORKER_REV='shared-family-v1';

const COLOR_ASPECT=1.68;
const COLOR_OCCUPANCY=.92;
const NEUTRAL_OCCUPANCY=.94;
const NEUTRAL_GAP=2;
const HUE_ABSOLUTE_WEIGHT=.28;
const LIGHT_ABSOLUTE_WEIGHT=.82;
const PACK_GAP=.004;
const PACK_SECTION_GAP=.05;
const PACK_MIN_RATIO=.5;
const PACK_MAX_RATIO=2;
const PACK_MAX_ROW_HEIGHT=1.08;
const PACK_MIN_ROW_HEIGHT=.32;
const EXTREME_RATIO=4;
const ATOMIC_CHUNK=24;

const post=(type,payload={})=>self.postMessage({type,...payload});
const abort=()=>{if(canceled)throw new DOMException('Aborted','AbortError')};
const clamp=(value,low=0,high=1)=>Math.max(low,Math.min(high,value));
const hashNoise=value=>{let hash=2166136261;for(let i=0;i<value.length;i++){hash^=value.charCodeAt(i);hash=Math.imul(hash,16777619)}return(hash>>>0)/4294967295};

function openDb(){return new Promise((resolve,reject)=>{const request=indexedDB.open(VIS_DB,VIS_VERSION);request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error)})}
function rowColor(row){const sorted=row?.aiSortColorVersion===AI_COLOR_VERSION?row.aiSortColor:null;if(sorted&&Number.isFinite(Number(sorted.l)))return{h:((Number(sorted.h)||0)%1+1)%1,l:clamp(Number(sorted.l)||0),c:Math.max(0,Number(sorted.c)||0),f:clamp(Number(sorted.f)||0)};const flow=row?.aiColorFlow;if(flow&&Number.isFinite(Number(flow.l)))return{h:((Number(flow.h)||0)%1+1)%1,l:clamp(Number(flow.l)||0),c:Math.max(0,Number(flow.c)||0),f:clamp(Number(flow.f)||0)};const color=row?.visualColor,feature=row?.visualFeature;if(!color&&!feature)return null;return{h:((Number(color?.dominantHue)||0)%1+1)%1,l:clamp((Number(feature?.meanLuma)||127)/255),c:Math.max(0,Number(color?.meanChroma)||0),f:clamp(Number(color?.colorFraction)||0)}}
async function loadColors(media){const byHash=new Map(media.map((item,index)=>[String(item.hash),index])),colors=Array(media.length).fill(null);let db;try{db=await openDb();if(!db.objectStoreNames.contains(VIS_STORE))return colors;post('progress',{done:0,total:media.length,detail:'Reading perceptual color…',stage:'visual'});const request=db.transaction(VIS_STORE,'readonly').objectStore(VIS_STORE).openCursor();let found=0;await new Promise((resolve,reject)=>{request.onerror=()=>reject(request.error);request.onsuccess=()=>{abort();const cursor=request.result;if(!cursor)return resolve();const row=cursor.value||{},index=byHash.get(String(row.hash||''));if(index!=null){const color=rowColor(row);if(color){colors[index]=color;found++}}if(found&&found%5000===0)post('progress',{done:found,total:media.length,detail:`Reading perceptual color · ${found.toLocaleString()} matched…`,stage:'visual'});cursor.continue()}});return colors}catch(error){if(error?.name==='AbortError')throw error;return colors}finally{db?.close?.()}}

function loadFamilies(media){return new Promise((resolve,reject)=>{const worker=new Worker(new URL(`./ai-family-engine-worker.js?v=${FAMILY_WORKER_REV}`,import.meta.url),{type:'module'});familyWorker=worker;let done=false;const finish=(fn,value)=>{if(done)return;done=true;if(familyWorker===worker)familyWorker=null;try{worker.terminate()}catch{}fn(value)};worker.onerror=event=>finish(reject,new Error(event.message||'Shared family worker failed'));worker.onmessageerror=()=>finish(reject,new Error('Shared family worker returned unreadable data'));worker.onmessage=event=>{const data=event.data||{};if(data.type==='progress'){post('progress',{...data,detail:data.detail?`Families · ${data.detail}`:'Finding shared families…'});return}if(data.type==='error'){const error=new Error(data.error||'Shared family worker failed');if(data.aborted)error.name='AbortError';finish(reject,error);return}if(data.type==='result')finish(resolve,data.result||{})};worker.postMessage({action:'build',payload:{media}})})}
function groupsFromIds(ids,count){const grouped=new Map();if(!ids?.length)return[];for(let index=0;index<Math.min(count,ids.length);index++){const id=Number(ids[index]);if(id<0)continue;let group=grouped.get(id);if(!group)grouped.set(id,group=[]);group.push(index)}return[...grouped.values()].filter(group=>group.length>1)}

function neutral(color){return!color||color.c<.032||color.f<.15}
function hueSeam(colors){const bins=64,hist=new Float64Array(bins);let total=0;for(const color of colors){if(neutral(color))continue;const weight=(.15+Math.min(1,color.c*7))*(.3+.7*color.f);hist[Math.min(bins-1,Math.floor(color.h*bins))]+=weight;total+=weight}if(total<=0)return 0;let best=0,bestScore=Infinity;for(let bin=0;bin<bins;bin++){const score=hist[(bin+bins-2)%bins]*.25+hist[(bin+bins-1)%bins]*.7+hist[bin]*1.25+hist[(bin+1)%bins]*.7+hist[(bin+2)%bins]*.25;if(score<bestScore){bestScore=score;best=bin}}return(best+.5)/bins}
function rankMap(indexes,valueFor){const sorted=indexes.slice().sort((a,b)=>valueFor(a)-valueFor(b)||a-b),length=indexes.reduce((max,index)=>Math.max(max,index+1),1),out=new Float32Array(length),denominator=Math.max(1,sorted.length-1);for(let rank=0;rank<sorted.length;rank++)out[sorted[rank]]=rank/denominator;return out}
function axisTargets(colors,indexes,seam){const hueValue=index=>((colors[index]?.h||0)-seam+1)%1,hueRank=rankMap(indexes,hueValue),lightRank=rankMap(indexes,index=>colors[index]?.l??.5),tx=new Float32Array(colors.length),ty=new Float32Array(colors.length);for(const index of indexes){const color=colors[index]||{h:0,l:.5},hue=hueValue(index),light=color.l;tx[index]=HUE_ABSOLUTE_WEIGHT*hue+(1-HUE_ABSOLUTE_WEIGHT)*hueRank[index];ty[index]=LIGHT_ABSOLUTE_WEIGHT*(1-light)+(1-LIGHT_ABSOLUTE_WEIGHT)*(1-lightRank[index])}return{tx,ty}}
function nearestFree(used,cols,rows,targetX,targetY,blocked=null){const available=(x,y)=>{const slot=y*cols+x;return!used[slot]&&!(blocked?.[slot])},cx=Math.max(0,Math.min(cols-1,Math.round(targetX))),cy=Math.max(0,Math.min(rows-1,Math.round(targetY)));if(available(cx,cy))return[cx,cy];const maxRadius=Math.max(cols,rows);let bestX=cx,bestY=cy,bestScore=Infinity;for(let radius=1;radius<=maxRadius;radius++){let found=false;const left=Math.max(0,cx-radius),right=Math.min(cols-1,cx+radius),top=Math.max(0,cy-radius),bottom=Math.min(rows-1,cy+radius);const consider=(x,y)=>{if(!available(x,y))return;const dx=x-targetX,dy=y-targetY,score=dx*dx+dy*dy*1.2;if(score<bestScore){bestScore=score;bestX=x;bestY=y;found=true}};for(let x=left;x<=right;x++){consider(x,top);if(bottom!==top)consider(x,bottom)}for(let y=top+1;y<bottom;y++){consider(left,y);if(right!==left)consider(right,y)}if(found)return[bestX,bestY]}return blocked?nearestFree(used,cols,rows,targetX,targetY,null):[cx,cy]}
function reserveCompactFamily(reserved,used,cols,rows,bounds,count){if(count<3||bounds.maxX<bounds.minX||bounds.maxY<bounds.minY)return false;const width=bounds.maxX-bounds.minX+1,height=bounds.maxY-bounds.minY+1,area=width*height;if(area>Math.ceil(count*1.65))return false;for(let py=bounds.minY;py<=bounds.maxY;py++)for(let px=bounds.minX;px<=bounds.maxX;px++){const slot=py*cols+px;if(!used[slot])reserved[slot]=1}return true}

function familyInfo(indexes,familyIds,order,media){const orderRank=new Int32Array(media.length);orderRank.fill(1e9);if(Array.isArray(order)){const byHash=new Map(media.map((item,index)=>[item.hash,index]));for(let rank=0;rank<order.length;rank++){const index=byHash.get(String(order[rank]));if(index!=null)orderRank[index]=rank}}const groups=new Map(),singles=[];for(const index of indexes){const id=Number(familyIds?.[index]);if(Number.isInteger(id)&&id>=0){let members=groups.get(id);if(!members)groups.set(id,members=[]);members.push(index)}else singles.push(index)}const multi=[];for(const members of groups.values()){members.sort((a,b)=>orderRank[a]-orderRank[b]||a-b);if(members.length>1)multi.push(members);else singles.push(members[0])}multi.sort((a,b)=>orderRank[a[Math.floor(a.length/2)]]-orderRank[b[Math.floor(b.length/2)]]||b.length-a.length||a[0]-b[0]);singles.sort((a,b)=>orderRank[a]-orderRank[b]||a-b);return{multi,singles}}
function familyStrength(members,tx,ty){let mx=0,my=0;for(const index of members){mx+=tx[index];my+=ty[index]}mx/=members.length;my/=members.length;let spreadX=0,spreadY=0;for(const index of members){spreadX+=Math.abs(tx[index]-mx);spreadY+=Math.abs(ty[index]-my)}spreadX/=members.length;spreadY/=members.length;const xStrength=spreadX<.055?.92:spreadX<.11?.80:spreadX<.18?.60:spreadX<.28?.38:.16,yStrength=spreadY<.08?.82:spreadY<.18?.64:spreadY<.28?.42:.22;return{mx,my,xStrength,yStrength}}
function placeColorField(media,colors,indexes,seam,familyIds,order,x,y){if(!indexes.length)return{cols:0,rows:0,familyGroups:0,reservedFamilies:0};const slots=Math.max(indexes.length,Math.ceil(indexes.length/COLOR_OCCUPANCY)),cols=Math.max(16,Math.ceil(Math.sqrt(slots*COLOR_ASPECT))),rows=Math.max(1,Math.ceil(slots/cols)),used=new Uint8Array(cols*rows),reserved=new Uint8Array(cols*rows),{tx,ty}=axisTargets(colors,indexes,seam),{multi,singles}=familyInfo(indexes,familyIds,order,media);let placed=0,reservedFamilies=0;const put=(index,nx,ny)=>{const[px,py]=nearestFree(used,cols,rows,nx*Math.max(0,cols-1),ny*Math.max(0,rows-1),reserved);used[py*cols+px]=1;x[index]=px;y[index]=py;if(++placed%5000===0)post('progress',{done:placed,total:media.length,detail:`Placing color field · ${placed.toLocaleString()}…`,stage:'layout'});return[px,py]};for(const members of multi){abort();const family=familyStrength(members,tx,ty),bounds={minX:cols,minY:rows,maxX:-1,maxY:-1};for(let position=0;position<members.length;position++){const index=members[position],ring=position?Math.sqrt(position)*.0025:0,angle=position*2.399963229728653,nx=family.mx+(tx[index]-family.mx)*(1-family.xStrength)+Math.cos(angle)*ring,ny=family.my+(ty[index]-family.my)*(1-family.yStrength)+Math.sin(angle)*ring*.7,[px,py]=put(index,clamp(nx),clamp(ny));bounds.minX=Math.min(bounds.minX,px);bounds.maxX=Math.max(bounds.maxX,px);bounds.minY=Math.min(bounds.minY,py);bounds.maxY=Math.max(bounds.maxY,py)}if(reserveCompactFamily(reserved,used,cols,rows,bounds,members.length))reservedFamilies++}singles.sort((a,b)=>{const ca=colors[a]||{},cb=colors[b]||{},va=(ca.c||0)*5+(ca.f||0),vb=(cb.c||0)*5+(cb.f||0);return vb-va||Math.abs((ca.l??.5)-.5)-Math.abs((cb.l??.5)-.5)||String(media[a].hash).localeCompare(String(media[b].hash))});for(const index of singles){abort();put(index,tx[index],ty[index])}return{cols,rows,familyGroups:multi.length,reservedFamilies}}
function placeNeutrals(media,colors,indexes,startX,familyIds,order,x,y){if(!indexes.length)return{cols:0,rows:0,familyGroups:0,reservedFamilies:0};const slots=Math.max(indexes.length,Math.ceil(indexes.length/NEUTRAL_OCCUPANCY)),cols=Math.max(4,Math.ceil(Math.sqrt(slots))),rows=Math.max(1,Math.ceil(slots/cols)),used=new Uint8Array(cols*rows),reserved=new Uint8Array(cols*rows),lightRank=rankMap(indexes,index=>colors[index]?.l??.5),{multi,singles}=familyInfo(indexes,familyIds,order,media),targetY=index=>LIGHT_ABSOLUTE_WEIGHT*(1-(colors[index]?.l??.5))+(1-LIGHT_ABSOLUTE_WEIGHT)*(1-lightRank[index]);let reservedFamilies=0;const placeGroup=(members,seed)=>{let meanY=0;for(const index of members)meanY+=targetY(index);meanY/=members.length;const baseX=.08+.84*hashNoise(String(seed)),bounds={minX:cols,minY:rows,maxX:-1,maxY:-1};for(let position=0;position<members.length;position++){const index=members[position],angle=position*2.399963229728653,ring=Math.sqrt(position)*.005,nx=clamp(baseX+Math.cos(angle)*ring),ny=clamp(meanY*.65+targetY(index)*.35+Math.sin(angle)*ring*.5),[px,py]=nearestFree(used,cols,rows,nx*Math.max(0,cols-1),ny*Math.max(0,rows-1),reserved);used[py*cols+px]=1;x[index]=startX+px;y[index]=py;bounds.minX=Math.min(bounds.minX,px);bounds.maxX=Math.max(bounds.maxX,px);bounds.minY=Math.min(bounds.minY,py);bounds.maxY=Math.max(bounds.maxY,py)}if(reserveCompactFamily(reserved,used,cols,rows,bounds,members.length))reservedFamilies++};for(const members of multi){abort();placeGroup(members,media[members[0]]?.hash||members[0])}for(const index of singles){abort();const nx=hashNoise(String(media[index]?.hash||index)),ny=targetY(index),[px,py]=nearestFree(used,cols,rows,nx*Math.max(0,cols-1),ny*Math.max(0,rows-1),reserved);used[py*cols+px]=1;x[index]=startX+px;y[index]=py}return{cols,rows,familyGroups:multi.length,reservedFamilies}}

function mediaRatio(item){const width=Math.max(1,Number(item?.width)||1),height=Math.max(1,Number(item?.height)||1);return width/height}
function layoutRatio(item){return Math.max(PACK_MIN_RATIO,Math.min(PACK_MAX_RATIO,mediaRatio(item)))}
function fitAspect(item,boxW,boxH){const source=mediaRatio(item);if(source>=EXTREME_RATIO||source<=1/EXTREME_RATIO)return[boxW,boxH];const boxRatio=boxW/Math.max(.0001,boxH);return source>=boxRatio?[boxW,boxW/source]:[boxH*source,boxH]}
function packingIds(hardGroups,logicalX,logicalY,count){const ids=new Int32Array(count);ids.fill(-1);let next=0;for(const group of hardGroups){const ordered=group.slice().sort((a,b)=>(Number(logicalY[a])||0)-(Number(logicalY[b])||0)||(Number(logicalX[a])||0)-(Number(logicalX[b])||0)||a-b);for(let start=0;start<ordered.length;start+=ATOMIC_CHUNK){const id=next++;for(const index of ordered.slice(start,start+ATOMIC_CHUNK))ids[index]=id}}return ids}
function blockInfo(indexes,packingGroupIds,logicalX,logicalY){const groups=new Map();for(const index of indexes){const id=Number(packingGroupIds?.[index]);if(id<0)continue;let members=groups.get(id);if(!members)groups.set(id,members=[]);members.push(index)}const rowFor=new Map(),xFor=new Map();for(const[id,members]of groups){if(members.length<2)continue;let x=0,y=0;for(const index of members){x+=Number(logicalX[index])||0;y+=Number(logicalY[index])||0}x/=members.length;y/=members.length;rowFor.set(id,Math.round(y));xFor.set(id,x)}return{groups,rowFor,xFor}}
function sortRow(row,packingGroupIds,logicalX,info){const blocks=new Map(),singles=[];for(const index of row){const id=Number(packingGroupIds?.[index]);if(id>=0&&info.groups.get(id)?.length>1){let members=blocks.get(id);if(!members)blocks.set(id,members=[]);members.push(index)}else singles.push(index)}const entries=singles.map(index=>({x:Number(logicalX[index])||0,members:[index]}));for(const[id,members]of blocks){members.sort((a,b)=>(Number(logicalX[a])||0)-(Number(logicalX[b])||0)||a-b);entries.push({x:info.xFor.get(id)??0,members})}entries.sort((a,b)=>a.x-b.x||a.members[0]-b.members[0]);return entries.flatMap(entry=>entry.members)}
function logicalRows(indexes,logicalX,logicalY,packingGroupIds){const info=blockInfo(indexes,packingGroupIds,logicalX,logicalY),grouped=new Map();for(const index of indexes){const id=Number(packingGroupIds?.[index]),key=id>=0&&info.rowFor.has(id)?info.rowFor.get(id):Math.round(Number(logicalY[index])||0);let row=grouped.get(key);if(!row)grouped.set(key,row=[]);row.push(index)}return{rows:[...grouped.entries()].sort((a,b)=>a[0]-b[0]).map(([,row])=>sortRow(row,packingGroupIds,logicalX,info)),info}}
function packSection(media,indexes,logicalX,logicalY,packingGroupIds,aspect,originX,outX,outY,renderW,renderH){if(!indexes.length)return{width:0,height:0,rows:0};const totalRatio=indexes.reduce((sum,index)=>sum+layoutRatio(media[index]),0),targetWidth=Math.max(4,Math.sqrt(Math.max(1,totalRatio)*Math.max(.7,Number(aspect)||1)));let{rows,info}=logicalRows(indexes,logicalX,logicalY,packingGroupIds);if(rows.length>1){const merged=[];let pending=[],pendingRatio=0;const flush=()=>{if(!pending.length)return;merged.push(sortRow(pending,packingGroupIds,logicalX,info));pending=[];pendingRatio=0};for(let rowIndex=0;rowIndex<rows.length;rowIndex++){const row=rows[rowIndex],rowRatio=row.reduce((sum,index)=>sum+layoutRatio(media[index]),0);if(rowRatio>=targetWidth*.52){flush();merged.push(row);continue}pending.push(...row);pendingRatio+=rowRatio;if(pendingRatio>=targetWidth*.78||rowIndex===rows.length-1)flush()}rows=merged}let top=.02,maxRight=originX;for(const row of rows){abort();const sum=row.reduce((total,index)=>total+layoutRatio(media[index]),0),gaps=PACK_GAP*Math.max(0,row.length-1),usable=Math.max(.1,targetWidth-gaps),height=Math.max(PACK_MIN_ROW_HEIGHT,Math.min(PACK_MAX_ROW_HEIGHT,usable/Math.max(.001,sum))),rowWidth=sum*height+gaps;let left=originX+Math.max(0,(targetWidth-rowWidth)/2);for(const index of row){const boxW=layoutRatio(media[index])*height,[width,actualHeight]=fitAspect(media[index],boxW,height),centerX=left+boxW/2,centerY=top+height/2;outX[index]=centerX-.5;outY[index]=centerY-.5;renderW[index]=width;renderH[index]=actualHeight;left+=boxW+PACK_GAP}maxRight=Math.max(maxRight,left-PACK_GAP);top+=height+PACK_GAP}return{width:Math.max(targetWidth,maxRight-originX),height:Math.max(0,top-PACK_GAP),rows:rows.length}}

async function build(media){const usable=Array.isArray(media)?media.filter(item=>HASH_RE.test(String(item?.hash||''))):[];if(!usable.length)return{kind:'map',x:new Float32Array(),y:new Float32Array(),renderW:new Float32Array(),renderH:new Float32Array(),worldW:1,worldH:1,labels:[],detail:'Color Map · no media'};const[colors,families]=await Promise.all([loadColors(usable),loadFamilies(usable)]);abort();const familyIds=families?.familyIds||new Int32Array(usable.length),hardGroups=groupsFromIds(families?.hardIds,usable.length),colorful=[],neutrals=[];for(let index=0;index<usable.length;index++)(neutral(colors[index])?neutrals:colorful).push(index);const seam=hueSeam(colors),logicalX=new Float32Array(usable.length),logicalY=new Float32Array(usable.length);logicalX.fill(-1);logicalY.fill(-1);post('progress',{done:0,total:usable.length,detail:'Building hue × lightness field with shared families…',stage:'layout'});const field=placeColorField(usable,colors,colorful,seam,familyIds,families?.order,logicalX,logicalY),neutralStart=field.cols?field.cols+NEUTRAL_GAP:0,neutralField=placeNeutrals(usable,colors,neutrals,neutralStart,familyIds,families?.order,logicalX,logicalY),atomicIds=packingIds(hardGroups,logicalX,logicalY,usable.length);post('progress',{done:usable.length,total:usable.length,detail:'Packing dense rows without splitting hard visual families…',stage:'packing'});const x=new Float32Array(usable.length),y=new Float32Array(usable.length),renderW=new Float32Array(usable.length),renderH=new Float32Array(usable.length);x.fill(-1);y.fill(-1);const colorPack=packSection(usable,colorful,logicalX,logicalY,atomicIds,COLOR_ASPECT,.02,x,y,renderW,renderH),neutralOrigin=.02+colorPack.width+(neutrals.length?PACK_SECTION_GAP:0),neutralPack=packSection(usable,neutrals,logicalX,logicalY,atomicIds,1,neutralOrigin,x,y,renderW,renderH),worldW=Math.max(1,.04+colorPack.width+(neutrals.length?PACK_SECTION_GAP+neutralPack.width:0)),worldH=Math.max(1,.04,Math.max(colorPack.height,neutralPack.height)+.04);return{kind:'map',x,y,renderW,renderH,worldW,worldH,labels:[],preserveRows:true,detail:`Color Map · adaptive hue × lightness · ${Number(families?.families||0).toLocaleString()} shared families · ${Number(families?.hardFamilies||0).toLocaleString()} hard visual groups · ${neutrals.length.toLocaleString()} neutral`}}

self.onmessage=async event=>{const data=event.data||{};if(data.action==='cancel'){canceled=true;try{familyWorker?.postMessage({action:'cancel'})}catch{}try{familyWorker?.terminate()}catch{}familyWorker=null;return}if(data.action!=='build')return;canceled=false;try{const media=Array.isArray(data.payload?.media)?data.payload.media:[],result=await build(media);if(canceled)return;const transfer=[];for(const key of['x','y','renderW','renderH'])if(result[key]?.buffer)transfer.push(result[key].buffer);self.postMessage({type:'result',result},transfer)}catch(error){post('error',{error:error?.name==='AbortError'?'Canceled':(error?.message||String(error)),aborted:error?.name==='AbortError'})}};
